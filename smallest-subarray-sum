    int[] arr = {2,1,5,2,3,2};
     int  target = 7;
     int  low=0; int sum = 0 ; int min = Integer.MAX_VALUE;
        for (int i = 0; i< arr.length ; i++) {
            sum+=arr[i];
            while(sum>=target){
                int len = i-low+1;
                min=Math.min(min,len);
                sum-=arr[low];
                low++;
            }

        }
        System.out.println(min);
